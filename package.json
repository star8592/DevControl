{
  "name": "devcontrol",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check lib/project-registry.mjs && node -e \"JSON.parse(require('fs').readFileSync('config/projects.json','utf8')); console.log('project registry json ok')\""
  },
  "engines": {
    "node": ">=20"
  }
}
